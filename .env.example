# Copy this file to .env.local and fill in real values.
# .env.local is gitignored — it stays on your machine only.
# CI builds do NOT read it; they use the DIRECTUS_URL / DIRECTUS_TOKEN
# GitHub repository secrets.
DIRECTUS_URL=https://your-directus.example.com
DIRECTUS_TOKEN=your-service-token
