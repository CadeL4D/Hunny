// Build-time configuration. CI overwrites this file with real values from the
// DIRECTUS_URL and DIRECTUS_TOKEN repository secrets just before deploying to
// GitHub Pages — the committed copy stays empty so neither ever appears in a
// public commit.
window.HUNNY_CONFIG = {
  baseURL: "",
  token: "",
};
